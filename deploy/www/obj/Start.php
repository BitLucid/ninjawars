<?php
// *** Need some include code to dynamically set an array of request variables from an initial array that could be added before the Start() initialization on each page.

/*	
 *	Single location designed to deal with the initial take-in of only acceptable Get, Post, and Session variables, etc.
 *  Also should deal with any sanitization of the variables that is needed (by filtering them).
 *  Should probably just replace most of the contents of the "header.php" file, as well.
 */
class Start
{


// *** Perhaps a dynamic method that accepts an array of parameters by reference here.
}


?>
